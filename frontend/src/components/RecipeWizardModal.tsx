import type { Categoria, Dificultad, Ingrediente, Origen, Receta, Usuario } from '../types';
interface Props { initialRecipe: Receta | null; categorias: Categoria[]; origenes: Origen[]; dificultades: Dificultad[]; ingredientesCatalogo: Ingrediente[]; currentUser: Usuario; onClose: () => void; onSaveRecipe: (recipe: Partial<Receta>) => void; onCreateIngredient: (name: string, unit: string) => Ingrediente; }
export default function RecipeWizardModal({ initialRecipe, onClose, onSaveRecipe }: Props) { return <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><section className="rounded-xl bg-white p-6"><h2 className="text-xl font-bold">{initialRecipe ? 'Editar receta' : 'Nueva receta'}</h2><div className="mt-4 flex gap-2"><button onClick={() => onSaveRecipe({})}>Guardar</button><button onClick={onClose}>Cancelar</button></div></section></div>; }
