import type { Categoria, Dificultad, Origen, Receta } from '../types';
interface Props { receta: Receta; categorias: Categoria[]; origenes: Origen[]; dificultades: Dificultad[]; onSelect: (recipe: Receta) => void; onToggleFavorite: (id: string) => void; onStartCooking: (recipe: Receta) => void; }
export default function RecipeCard({ receta, onSelect, onToggleFavorite, onStartCooking }: Props) { return <article className="rounded-xl border bg-white p-4"><h3 className="font-bold">{receta.plato.nombre}</h3><p className="text-sm">{receta.plato.descripcion}</p><div className="mt-3 flex gap-2"><button onClick={() => onSelect(receta)}>Ver</button><button onClick={() => onStartCooking(receta)}>Cocinar</button><button onClick={() => onToggleFavorite(receta.id)}>{receta.esFavorito ? 'Quitar favorito' : 'Favorito'}</button></div></article>; }
