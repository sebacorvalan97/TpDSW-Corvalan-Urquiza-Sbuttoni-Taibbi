import type { Categoria, Comentario, Dificultad, Ingrediente, Origen, Receta, Usuario } from '../types';
interface Props { receta: Receta; categorias: Categoria[]; origenes: Origen[]; dificultades: Dificultad[]; todosIngredientes: Ingrediente[]; comentarios: Comentario[]; currentUser: Usuario; onClose: () => void; onToggleFavorite: (id: string) => void; onAddComment: (id: string, rating: number, text: string) => void; onLikeComment: (id: string) => void; onEditRecipe: (recipe: Receta) => void; onDeleteRecipe: (id: string) => void; initialCookingMode: boolean; }
export default function RecipeDetailModal({ receta, onClose, onToggleFavorite, onEditRecipe, onDeleteRecipe }: Props) { return <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><section className="max-w-lg rounded-xl bg-white p-6"><h2 className="text-2xl font-bold">{receta.plato.nombre}</h2><p>{receta.plato.descripcion}</p><div className="mt-4 flex gap-2"><button onClick={() => onToggleFavorite(receta.id)}>Favorito</button><button onClick={() => onEditRecipe(receta)}>Editar</button><button onClick={() => onDeleteRecipe(receta.id)}>Eliminar</button><button onClick={onClose}>Cerrar</button></div></section></div>; }
