import type { Usuario } from '../types';
type Tab = 'recipes' | 'ranking' | 'create' | 'admin' | 'substitutes' | 'favorites';
interface Props { currentTab: Tab; setCurrentTab: (tab: Tab) => void; currentUser: Usuario; favoritesCount: number; onSelectTag: (tag: string) => void; }
export default function Sidebar({ setCurrentTab, favoritesCount, onSelectTag }: Props) { return <aside className="hidden w-44 shrink-0 space-y-2 lg:block"><button className="block w-full text-left" onClick={() => setCurrentTab('recipes')}>Descubrir</button><button className="block w-full text-left" onClick={() => setCurrentTab('favorites')}>Favoritos ({favoritesCount})</button><button className="block w-full text-left" onClick={() => setCurrentTab('ranking')}>Ranking</button><button className="block w-full text-left" onClick={() => setCurrentTab('substitutes')}>Despensa</button><button className="block w-full text-left" onClick={() => setCurrentTab('admin')}>Administración</button><button className="block w-full text-left" onClick={() => onSelectTag('saludable')}>Saludable</button></aside>; }
